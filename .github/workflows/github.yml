name: first_workflow
on: [push]
jobs:
  cypress-run:
    runs-on: [ubuntu-latest]
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: cypress run
        uses: cypress-io/github-action@v6
        with: 
           build: npm run build
           start: npm start


